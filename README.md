# KiRATE

Kinetics Informed Rate Tuning Environment

## Install
1. Clone this repository using `git`.
    ```bash
    git clone https://github.com/tdinelli/diffPLOG2TROE
    ```
2. Navigate into the directory containing the code and install the package by running the following command:
    ```bash
    pip install .
    ```
